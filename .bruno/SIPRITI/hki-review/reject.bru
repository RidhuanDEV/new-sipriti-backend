meta {
  name: reject
  type: http
  seq: 6
}

post {
  url: {{host}}/api/hki-review/:id/reject
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

params:path {
  id: {{hki_id}}
}

body:json {
  {
    "catatan": "Dokumen belum lengkap"
  }
}

docs {
  # Reject HKI

  ## API Endpoint
  `POST /api/hki-review/:id/reject`

  ## Request Body
  | Field | Type | Required | Description |
  |-------|------|----------|-------------|
  | catatan | string | ✅ | Catatan penolakan wajib diisi |
}
